Alternative calculation for `double (double x)`.

Solution:
```
double (double 2)
  = {applying double}
      double (2 + 2)
  = {applying double}
      (2 + 2) + (2 + 2)
  = {applying +}
      4 + (2 + 2)
  = {applying +}
      4 + 4
  = {applying +}
      8
```
